template(name="index")
  .col-50
    if compare recent.count '||' uploads
      .wh100.scroll-wrap
        each uploads
          +uploadRow
        each recent.each
          +listingRow
    else
      .info-block
        .h2 you have no uploaded files
        p
          | start with 
          a(data-select-file href="#" title="Select file") selecting a file
          br
          b drag'n'drop file(s) or directory