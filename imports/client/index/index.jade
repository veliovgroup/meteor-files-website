template(name="index")
  .col-50
    if compare recent.count '||' uploads
      .wh100.scroll-wrap
        each uploads
          +uploadRow
        each recent.each
          +listingRow
    else
      .text-center
        .h3 You have no uploaded files
        div
          | Start with 
          a(data-select-file href="#" title="Select file") selecting a file
          br
          | or drag'n'drop file(s) or directory